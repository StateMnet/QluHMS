package com.ruoyi.hotel.enums;

/*
public enum RoomType {
    SINGLE(1l, "单人间"),
    DOUBLE(2l, "双人间"),
    SUITE(3l, "套房"),
    DELUXE(4l, "豪华间");

    private final Long code;
    private final String info;

    RoomType(Long code, String info) {
        this.code = code;
        this.info = info;
    }

    public Long getCode() {
        return code;
    }

    public String getInfo() {
        return info;
    }

    public static RoomType getByCode(Long code) {
        for (RoomType type : RoomType.values()) {
            if (type.getCode().equals(code)) {
                return type;
            }
        }
        return null;
    }
}*/
